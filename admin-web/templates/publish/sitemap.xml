<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{% for page in pages %}    <url><loc>{{ origin }}{{ page.path }}</loc></url>
{% endfor %}{% for post in posts %}    <url><loc>{{ origin }}/blog/{{ post.slug }}</loc><lastmod>{{ post.updated_at|date:"c" }}</lastmod></url>
{% endfor %}
</urlset>
